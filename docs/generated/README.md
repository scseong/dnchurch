# generated/ — 자동 생성 파일

> **🚫 이 디렉토리의 파일은 손으로 수정하지 않는다.** 변경은 항상 생성 스크립트를 통해 일어난다.

## 무엇이 어떻게 생성되는가

| 파일 | 생성 방법 | 트리거 |
| --- | --- | --- |
| `db-schema.md` (예정) | (Phase 2에서 정의) | Supabase 스키마 변경 후 |

(현재는 placeholder. Phase 2에서 채움)

## 왜 자동 생성인가

- 코드와 DB가 진실의 원천(source of truth)이고, 이 디렉토리는 **사람·에이전트가 읽기 쉬운 미러**다
- TypeScript로만 표현된 `database.types.ts`를 보완하여, 에이전트가 스키마·관계·RLS를 빠르게 파악하도록 함

## 변경이 필요할 때

손으로 고치지 말고 **생성 스크립트를 수정**한 뒤 다시 실행. 만약 출력 형식 자체를 바꾸고 싶다면 ADR로 결정 기록.

<!-- last-audit: 2026-05-01 -->
